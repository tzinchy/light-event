# @light-event/shared-types

Типобезопасный TS-клиент, генерируемый из OpenAPI-схемы FastAPI (`@hey-api/openapi-ts`).

Пакет наполняется на этапе фронтенда (PLAN.md §9, п.14): `make openapi-client` выгружает `openapi.json`, генерация кладёт клиент в `src/`.
